import React from 'react';
import ReactDOM from 'react-dom/client';
import QueueSimulator from './QueueSimulator.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <QueueSimulator />
  </React.StrictMode>
);
